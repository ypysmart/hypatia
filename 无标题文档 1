1、更换ns3为3.32，删除simulator下的doc、build、building文件夹
2、将quic文件夹移植进入contrib文件夹内，在将mpquic的application改名为quic-application并放入contrib，修改wscript增加cc和h模块并且安装
sudo apt update
sudo apt install libeigen3-dev，

3、修改main_satnet.cc、satellite-network文件夹的helper和model
4、修改mpquicbuclksendapplication的ifndef
5、找到文件路径：通常在 sgp4unit.h 
将 #define pi 3.14159265358979323846 改为 const double pi = 3.14159265358979323846;（使用常量代替宏）。
6、删除application和quic-application重复的模块，可能要给几个模块的头文件增加"ns3/"要不汇报错。
bash hypatia_build.sh
conda activate ypy

cd ~/hypatia/paper/ns3_experiments/a_b/
python step_1_generate_runs.py

python step_2_run.py


python step_3_generate_plots.py

        m_idleTimeoutEvent = Simulator::Schedule (m_idleTimeout, &QuicSocketBase::Close, this);//这句原来没注释
      m_idleTimeoutEvent = Simulator::Schedule (m_idleTimeout, &QuicSocketBase::Close, this);//这里调用了close函数

